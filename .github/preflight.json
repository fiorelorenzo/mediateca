{
  "base": "origin/main",
  "jobs": 3,
  "checks": [
    {
      "id": "admin-app",
      "run": "cd admin-app && npm ci && npm run lint && npm run typecheck && npm run test",
      "when": ["admin-app/**"],
      "why": "mirrors the admin-app CI job: eslint, tsc --noEmit, vitest unit tests"
    },
    {
      "id": "orchestrator",
      "run": "cd orchestrator && uv venv .venv --python 3.12 --clear && uv pip install --python .venv -r requirements.txt -r requirements-dev.txt && .venv/bin/ruff check src tests && .venv/bin/mypy && .venv/bin/pytest",
      "when": ["orchestrator/**"],
      "why": "mirrors the orchestrator CI job: ruff, strict mypy, pytest (asyncio_mode=auto) in a scratch venv, same as CI installs requirements.txt + requirements-dev.txt outside uv.lock"
    },
    {
      "id": "hls-encoder",
      "run": "cd hls-encoder && uvx ruff@0.15.12 check --select E9,F63,F7,F82 encoder.py server.py",
      "when": ["hls-encoder/**"],
      "why": "mirrors the hls-encoder CI job: syntax/undefined-name errors only, this service carries no lint config"
    }
  ]
}
